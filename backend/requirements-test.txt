# Minimal dependency set for the offline unit/contract test suite (CI job "unit").
# Full runtime deps (torch, transformers, praw, yt-dlp, atproto, ...) live in
# requirements.txt and are only needed by the heavier e2e / model jobs.
pytest>=8.0
httpx>=0.27
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
bcrypt>=4.1
PyJWT>=2.8
python-dotenv>=1.0
email-validator>=2.1
cryptography>=42.0
alembic>=1.13
# backend.main imports numpy at module level, and its file-upload routes make
# FastAPI require python-multipart at import — the offline suite needs both.
numpy>=1.26
python-multipart>=0.0.6
